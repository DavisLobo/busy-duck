[project]
name = "busy-duck"
version = "0.1.0"
description = "Local calendar aggregation app"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
  "sqlalchemy>=2.0.0",
  "pydantic>=2.0.0",
  "python-dotenv>=1.0.1",
]